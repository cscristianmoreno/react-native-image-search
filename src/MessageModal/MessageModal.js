import { View, Text, StyleSheet } from "react-native";

const MessageModal = () => {
    return(
        <View style={style.classModalContainer}>

        </View>
    )
}

const style = StyleSheet.create({
    
})

export default MessageModal;